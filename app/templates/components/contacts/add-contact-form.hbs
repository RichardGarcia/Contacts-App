{{!-- {{yield}} --}}

<div class="row marginBottom15">
	<div class="col-xs-3">
		{{input type="text" value=name size="100" placeholder="Enter a new name" class="form-control"}}
	</div>

	<div class="col-xs-3">
		{{phone-input type="text" value=phone size="20" placeholder="Phone" class="form-control"}}
	</div>
	
	<div class="col-xs-3">
		{{input type="text" value=email size="50" placeholder="email address" class="form-control"}}
	</div>

	{{!-- <button type="submit" class="btn btn-warning" {{action 'addNewContact'}}>Add New Contact</button> --}}
	<button type="submit" class="btn btn-warning" {{action 'buttonClicked'}} disabled={{nothingThere}}>Add New Contact</button>
</div>